# Third-party dependencies.
#
# varroa_model.py and varroa_model_corrected.py use only the Python standard
# library (math, json, os, dataclasses, typing).  The single external
# dependency is openpyxl, used only by scripts/validate.py to read the
# reference workbook.  Tests use only stdlib unittest.
#
# Prefer installing via the project extras instead:
#     pip install -e ".[validate]"     # openpyxl only
#     pip install -e ".[dev]"          # + ruff

openpyxl>=3.1